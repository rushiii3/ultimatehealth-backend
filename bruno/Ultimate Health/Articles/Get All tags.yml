info:
  name: Get All tags
  type: http
  seq: 5

http:
  method: GET
  url: "{{baseUrl}}/api/articles/tags"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
