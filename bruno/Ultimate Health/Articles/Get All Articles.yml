info:
  name: Get All Articles
  type: http
  seq: 8

http:
  method: GET
  url: "{{baseUrl}}/api/articles"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
