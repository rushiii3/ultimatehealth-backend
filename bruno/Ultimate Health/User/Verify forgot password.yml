info:
  name: Verify forgot password
  type: http
  seq: 9

http:
  method: POST
  url: "{{baseUrl}}/api/user/verifypassword"
  body:
    type: json
    data: |-
      {
        "email": "test@gmail.com",
        "otp": "347152",
        "newPassword": "Password@123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
