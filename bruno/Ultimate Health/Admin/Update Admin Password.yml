info:
  name: Update Admin Password
  type: http
  seq: 10

http:
  method: PUT
  url: "{{baseUrl}}/api/v3/admin/update-password"
  body:
    type: json
    data: |-
      {
        "old_password": "Test@123",
        "new_password": "Password@123 " 
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
