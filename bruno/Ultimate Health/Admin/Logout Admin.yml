info:
  name: Logout Admin
  type: http
  seq: 8

http:
  method: POST
  url: "{{baseUrl}}/api/v3/admin/logout"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
