info:
  name: Register
  type: http
  seq: 1

http:
  method: POST
  url: "{{baseUrl}}/api/admin/register"
  body:
    type: json
    data: |-
      {
        "user_name": "test",
        "user_handle": "test",
        "email":"test@gmail.com",
        "password":"Test@123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
