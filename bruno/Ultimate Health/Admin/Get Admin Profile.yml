info:
  name: Get Admin Profile
  type: http
  seq: 7

http:
  method: GET
  url: "{{baseUrl}}/api/admin/getprofile"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
